# 寒区学校蒸压加气混凝土砌块砌筑准入质量闭环

## 项目目标

构建一个面向寒冷地区学校建设工程的 Go 后端服务，以生产任务为一致性边界，管理蒸压加气混凝土砌块从原料锁定、搅拌浇注、静停切割、釜车编组、蒸压冷却、尺寸及性能检测、异常复验、双人复核到砌筑准入的质量闭环。系统以只追加事件和物料谱系持久化全部质量事实，使用整数克、整数微米、逻辑时间及整数定点数保证跨平台确定性，并通过原子事务、限时互斥租约、幂等操作号、新旧复验代次隔离和单写终局屏障处理真实并发、故障与重启恢复。实现规模控制在20个以上生产Go文件、约2500有效生产行且总体位于2000—3000行，按不少于4个有实际职责的包组织；公开测试分布在至少4个测试文件并包含至少12个独立用例。

## 端到端业务流程

1. 任务锁定：创建工厂生产任务并在同一版本快照中固定粉煤灰或砂浆、水泥、石灰批次，铝粉悬浮液代次，配方摘要与回浆上限，搅拌盘计划、模具和坯体身份，静停温度程序、切割网格与钢丝寿命窗口，釜车位置与蒸压曲线，取样映射、检测阈值及任务代次；摘要陈旧、批次错配或身份重复时拒绝锁定。
2. 配料至浇注：客户端携带操作号、逻辑时间和有效设备租约提交原料计量、料浆搅拌、发气浇注证据；事务原子完成库存扣减、谱系节点及唯一父边创建、质量台账更新和租约占用，设备拒绝或超时仅登记可重试调用，不推进阶段。
3. 静停与切割：坯体依次完成发气、静停、脱模、纵切和横切；校验温度程序、发气膨胀率、整数微米切割网格、钢丝磨损窗口及完整长方体覆盖，随后把有效坯体唯一拆分为成品砌块、试样候选、边角料和合理损耗，并登记可回收废浆。
4. 编组与蒸压：砌块按锁定方案占用釜车唯一位置并进入蒸压釜；温压采集点按逻辑时间追加，校验升压、恒压、降压曲线及恒压等效积分，曲线缺口、时间倒序、位置冲突或租约过期均不得形成有效蒸压完成证据，重启后可从持久化时间轴继续。
5. 检测与异常复验：出釜冷却后按固定映射消费试样，记录尺寸偏差、干密度、含水率、抗压强度和冻融质量损失；塌模、欠发气、切割粘连、断丝、蒸压曲线缺口、出釜裂纹、密度偏离或强度不足触发一次性影响传播，按同搅拌盘、相邻釜车位置、共享钢丝磨损窗口和同原料批生成确定排序的唯一复验集合并创建新代次。
6. 双人复核与终局裁定：两名不同且具备资格的人员分别复核谱系、质量去向、阶段前缀、切割覆盖、蒸压时间轴和检测结论；仲裁器在单写终局屏障下竞争写入砌筑准入、质量隔离或取消之一，迟到旧代次结果与并发请求只能读取既有终局，不能改变当前结论。

## 核心组件与职责

1. 砌块配方与质量规则目录：维护带版本的原料要求、配方摘要、回浆上限、几何约束、钢丝寿命、蒸压程序、检测映射和质量阈值，并提供任务锁定快照及陈旧摘要校验。
2. 生产任务及物料谱系聚合：实现任务代次与坯体阶段状态机，持久化“原料批—搅拌盘—浇注坯体—切割砌块—试样与边角料”的只追加节点、唯一父边、去向和证据引用，检测成环、多父和越序。
3. 质量守恒与资源租约管理器：以整数克维护库存预留、扣减、回浆、产出、试样消耗、边角料与损耗平衡；为模具、搅拌机、静停位、切割线、釜车位置、蒸压釜和压力机提供带逻辑期限的互斥租约，并将扣减、建边、租约及幂等记录纳入同一事务。
4. 发气切割及蒸压证据记录器：验证设备调用状态、发气与静停序列、三维切割网格、钢丝使用和釜车编组，追加温压采集点并以安全整数定点算法计算膨胀率、切割收得率、升降压速率与恒压等效时长。
5. 性能复验与终局仲裁器：管理尺寸及性能试样消费、异常索引、复验集合生成、代次隔离、双人复核资格和终局屏障，保证旧证据永久可查而当前结论只由最新代次决定。
6. Go HTTP API：提供JSON命令与查询接口、事务边界、稳定错误响应、确定排序、健康检查和重启恢复入口；负责参数解码、操作号校验、超时控制及领域错误映射，不绕过领域聚合。

## 领域规则与不变量

1. 谱系只追加且必须为有向无环图；每个成品砌块只能来自一个锁定坯体，跨盘混合、切割拆分、试样消费及边角料去向各自保留唯一父边，任何历史节点、边和已消费去向不得覆盖或删除。
2. 每个坯体的有效阶段必须严格形成“配料、搅拌、浇注、发气、静停、脱模、纵切、横切、编组、蒸压、冷却”的前缀；重复阶段只允许同操作号同内容幂等读取，跳级、错误代次、时间倒序或无有效租约均不得追加有效证据。
3. 干料、水、铝粉液、回浆、有效坯体、成品、试样、边角料和合理损耗均使用整数克；每次转换满足输入质量等于全部输出与允许损耗之和，试样只能消费一次，回浆比例不得超过锁定上限，并发超领必须仅有一个事务成功。
4. 坯体及切割网格使用整数微米；所有砌块和槽口均为坯体边界内的非退化长方体，切割余量非负，体积并集必须完整覆盖锁定有效坯体且任意两块内部不重叠；坐标、长度及乘加运算必须在溢出前拒绝。
5. 水料比、回浆率、发气膨胀率、切割收得率、升降压速率、恒压等效时长、干密度、抗压强度和冻融质量损失率使用明确比例尺的整数定点运算，规定半远离零舍入，并拒绝负值、除零、非法区间、非正时间间隔及中间值溢出。
6. 所有共享设备和位置使用资源标识、持有者、获取逻辑时刻、到期逻辑时刻和租约令牌组成的限时互斥租约；续约不得复活已过期租约，释放必须匹配令牌，任务重启后租约按持久化逻辑时钟恢复并可确定判定过期。
7. 设备拒绝、断连、超时或格式错误只创建包含设备、请求摘要、逻辑时间、确定重试序号和状态的待重试调用；不得伪造测量值、消耗试样或推进阶段，相同操作号内容不同必须返回幂等冲突。
8. 异常复验集合以规范化异常类型、源证据和受影响砌块集合生成唯一键；传播范围是同盘、固定邻接表中的相邻釜车位置、共享钢丝磨损窗口和同原料批的确定并集，新代次覆盖该集合，旧代次迟到回执只能归档。
9. 只有最新代次的谱系与质量守恒、全部坯体阶段闭合、切割覆盖完整、蒸压曲线合格、尺寸及性能阈值合格、全部异常复验完成且两名不同合格人员独立通过时才能准入；准入、隔离和取消共享一个不可改写的终局槽。
10. 所有批量校验错误按工厂、生产批、搅拌盘、坯体、切割行列、釜车位置、砌块编号和代次升序输出；失败事务不得留下材料扣减、谱系边、租约、有效阶段证据、试样消耗、复核或准入凭据。

## 数据模型与持久化

1. RecipeRuleSnapshot：规则版本、配方摘要、原料类别及允许批次、各材料整数克范围、回浆上限、几何参数、钢丝寿命窗口、静停及蒸压程序、检测映射、质量阈值和锁定时间。
2. ProductionTask：工厂、生产批、当前代次、锁定摘要、搅拌盘计划、任务状态、逻辑时钟、创建版本及终局引用；TaskGeneration保存每代复验覆盖范围和当前性。
3. MaterialNode与LineageEdge：节点种类、稳定标识、质量克数、任务代次、来源操作和去向状态；边记录唯一父节点、子节点、转换类型及证据引用，并以唯一约束阻止多父归属。
4. BodyStageEvent与Evidence：坯体、阶段、序号、逻辑时间、设备调用、租约令牌、输入摘要和有效性；专用证据包含发气高度、静停温度、切割网格与钢丝组、釜车位置、温压点和冷却裂纹记录。
5. CutCell与SamplingMap：以整数微米保存坯体边界、行列层索引、砌块长宽高、槽口及余量；映射砌块到尺寸样、密度含水率样、抗压样、冻融样和边角料，并记录试样消费状态。
6. MassLedgerEntry：材料账户、整数克借贷方向、转换事务、谱系节点和操作号；事务提交后同一转换的借贷净额必须为零，库存账户不得为负。
7. ResourceLease与DeviceCall：资源类型及标识、持有者、令牌、获取和到期逻辑时间；设备调用保存请求摘要、重试次数、待重试或成功状态、原始格式校验结果及关联证据。
8. TestResult与RetestSet：试样、指标、整数定点值、比例尺、阈值结论、消费操作、异常来源、规范化影响成员、集合唯一键、代次及迟到标记。
9. Review与FinalVerdict：人员标识、资格快照、独立复核摘要、代次和签署逻辑时间；终局记录准入、隔离或取消类型，唯一凭据号、裁定原因、有序证据摘要和不可变写入版本。
10. IdempotencyRecord与EventLog：作用域、操作号、规范请求哈希、结果或稳定错误码、提交版本；事件日志按单调序号只追加，用于启动时重建聚合、恢复待重试调用及校验快照一致性。

## 公开接口

1. POST /v1/tasks：创建并锁定生产任务；请求包含规则版本、原料批、搅拌盘和全部生产检测计划，成功返回任务代次与锁定摘要，陈旧或错配返回稳定错误码。
2. POST /v1/tasks/{taskID}/commands：统一提交计量、搅拌、浇注、发气、静停、脱模、纵横切割、编组、蒸压、冷却和检测命令；要求操作号、预期版本、逻辑时间、租约令牌及阶段载荷。
3. POST /v1/tasks/{taskID}/leases 与 /leases/{token}/renew：原子获取或续约设备及位置租约；返回令牌、逻辑到期时刻和冲突持有范围，不暴露非确定墙钟判断。
4. POST /v1/tasks/{taskID}/device-calls/{callID}/attempts：登记脚本化设备尝试及成功读数或失败类别；服务器确定重试序号，格式错误和失败响应仅更新待重试调用。
5. POST /v1/tasks/{taskID}/retests：依据异常证据创建或幂等取得唯一复验集合和新代次；GET /v1/tasks/{taskID}/retests/{generation}查询成员、传播原因及旧代次迟到证据。
6. POST /v1/tasks/{taskID}/reviews 与 /verdicts：提交独立复核以及竞争性准入、隔离或取消请求；成功或失败均返回当前唯一终局及稳定版本。
7. GET /v1/tasks/{taskID}、/lineage、/mass-balance、/evidence 和 /verdict：按规范键排序查询当前状态、完整历史谱系、质量台账、阶段与温压证据以及终局凭据，支持重启后一致读取。
8. 错误响应统一包含code、message、operation_id、retryable和按规定业务键排序的reasons；核心错误码涵盖STALE_RULE、RAW_BATCH_MISMATCH、DUPLICATE_BODY、LINEAGE_CYCLE、MULTIPLE_PARENT、MATERIAL_OVERDRAW、RECLAIM_RATIO_EXCEEDED、STAGE_OUT_OF_ORDER、GRID_INVALID、WIRE_LIFE_EXCEEDED、LEASE_CONFLICT、LEASE_EXPIRED、LOGICAL_TIME_REVERSED、FIXED_POINT_OVERFLOW、DEVICE_RETRY_PENDING、GENERATION_CONFLICT、IDEMPOTENCY_CONFLICT及FINAL_ALREADY_SET。

## 失败边界

1. 存储事务边界覆盖幂等记录、库存与质量台账、谱系节点及边、资源租约、阶段事件、试样消费、复核和终局写入；任何验证、唯一约束、设备结果或并发版本冲突导致整体回滚。
2. 规则锁定前验证所有外键和摘要，锁定后生产命令只引用快照；目录后续更新不能改变在制任务，客户端提交旧预期版本时返回陈旧错误而不局部更新。
3. 设备适配边界只接收脚本化结果并验证格式，超时和断连转换为持久化待重试状态；恢复进程按调用标识和重试序号续作，不把未知结果解释为成功。
4. 几何、质量和温压算法先做符号、范围及安全乘加检查，再产生领域结果；任何溢出、除零或时间轴缺口使整条命令失败，不持久化派生值。
5. 复验创建以影响集合唯一键和任务当前代次做比较交换；并发创建只能有一个新代次，失败者读取获胜结果，旧代次的迟到检测不会触发新的终局计算。
6. 终局使用任务级唯一槽和预期版本比较交换；准入、隔离、取消并发时只有一个提交，其余返回FINAL_ALREADY_SET及既有凭据，进程崩溃重启后结果保持不变。

## 验收标准

1. 锁定接口一次性固化原料批、配方及回浆上限、盘与模具坯体计划、静停程序、切割网格、钢丝窗口、釜车编组、蒸压曲线、取样映射、阈值和代次；任何陈旧摘要、批次错配或重复坯体均以稳定代码失败且无任务残片。
2. 整数微米几何校验可证明公开坯体被合法砌块、槽口与切割余量完整且不重叠地覆盖，并确定拒绝缺块、重叠、越界、退化、负值和乘加溢出。
3. 并发配料或租约竞争中只有合法事务提交，整数克质量始终守恒；同操作号同内容重试返回相同结果，同操作号异内容以及任一建边、扣减或租约冲突均整体回滚。
4. 所有坯体严格按锁定阶段前缀推进；跳步、重复切割、错误钢丝窗口、错误代次、逻辑时间倒序或过期租约不会写入有效证据，且重启后阶段和租约判断一致。
5. 全部比率、体积与温压积分采用确定整数定点计算，在公开边界向量上得到精确值，并确定处理舍入、除零、非正时间间隔、区间端点、曲线缺口和溢出。
6. 六类设备的拒绝、断连、超时和格式错误只形成带逻辑时间及确定重试次数的待重试调用，不推进阶段或消耗试样；恢复后成功尝试仅提交一次真实读数。
7. 八类异常能按谱系、固定釜车邻接和钢丝窗口生成唯一且有序的复验集合；并发复验仅产生一个新代次，旧块去向、试样消费和温压证据永久保留，旧代次迟到结果不改变当前结论。
8. 只有全部闭环条件和两名不同合格人员复核满足时才生成唯一砌筑准入凭据；准入、隔离与取消同步竞争仅有一个终局。仓库包含go.mod、可编译Go后端、支持linux/amd64与linux/arm64的Docker构建文件，生产代码不少于20个Go文件且约2000—3000有效行，并有至少4个公开测试文件、12个测试用例及10条可独立设障的缺陷支线基线。

## 确定性测试场景

1. 使用固定工厂、原料克数与配方锁定任务；分别断言成功快照、陈旧摘要、粉煤灰或水泥批次错配、重复坯体身份及失败后零残留。
2. 建立公开谱系并尝试跨盘混合、同砌块双父和回指祖先；断言稳定错误码、拓扑无环、历史节点不变及错误原因规范排序。
3. 以固定整数微米坯体测试合法三维网格，再分别注入缺块、内部重叠、越界、零厚度、负槽口和体积乘法溢出。
4. 用同步屏障让两个操作同时超领同一原料批并争抢同一搅拌机或釜车位置；断言仅一方成功、库存不负、租约互斥且失败事务没有谱系边。
5. 对相同操作号执行同载荷重试和异载荷重试；断言前者复用原结果及版本，后者返回IDEMPOTENCY_CONFLICT且不新增事件。
6. 逐项验证坯体阶段前缀，并尝试跳过静停、重复横切、使用错误钢丝窗口、过期租约和倒序逻辑时间；断言有效事件序列完全不变。
7. 用精确质量向量验证干料、水、铝粉液、回浆到成品、试样、边角料和损耗的平衡，覆盖回浆率上限两侧、试样重复消费及中间乘加溢出。
8. 输入固定发气、静停和温压序列，精确断言膨胀率、升降压速率、恒压等效时长及边界舍入；再注入重复时刻、时间缺口、除零和积分溢出。
9. 脚本化电子秤和压力机依次返回拒绝、超时、格式错误和成功；断言重试次数确定，前三次不推进、不造读数、不耗样，重启后成功值仅提交一次。
10. 分别注入塌模、欠发气、粘连、断丝、曲线缺口、裂纹、密度偏离和强度不足；利用固定釜车邻接表与钢丝窗口断言传播成员及原因顺序完全一致。
11. 以同步屏障并发创建同一异常复验，断言只增加一个代次；随后提交旧代次迟到合格回执，断言仅归档且当前隔离状态不变。
12. 构造所有质量条件合格但复核人为同一人的任务，断言拒绝准入；补充第二名合格人员后并发提交准入、隔离和取消，断言唯一终局且重启后凭据一致。
13. 在事务提交前模拟持久化失败，分别覆盖材料扣减后、谱系建边后、租约获取后、试样消费后和终局写入前的故障点，断言恢复后不存在任何部分状态。

## 组件追踪关系

1. 砌块配方与质量规则目录对应任务锁定流程、RecipeRuleSnapshot、POST /v1/tasks，以及验收1和规则快照陈旧测试。
2. 生产任务及物料谱系聚合对应严格阶段前缀和只追加唯一父边，落到ProductionTask、MaterialNode、LineageEdge、BodyStageEvent及谱系无环、越序、重启恢复测试。
3. 质量守恒与资源租约管理器对应整数克守恒、并发超领、回浆上限和设备互斥，落到MassLedgerEntry、ResourceLease及原子竞争、幂等冲突、事务回滚测试。
4. 发气切割及蒸压证据记录器对应整数微米覆盖、钢丝寿命、釜车编组和温压定点积分，落到CutCell、SamplingMap、Evidence、DeviceCall及几何与时间轴边界测试。
5. 性能复验与终局仲裁器对应检测、异常传播、新旧代次隔离、双人复核和单写终局，落到TestResult、RetestSet、Review、FinalVerdict及传播、迟到回执和终局竞争测试。
6. Go HTTP API对应全部命令查询接口、稳定错误码、确定原因排序、操作号和预期版本处理，并由端到端HTTP测试覆盖无部分提交、可重试响应和重启后一致读取。

## 独特性

方案聚焦寒区学校砌体材料特有的蒸压加气混凝土制造链，把三维切割覆盖、钢丝磨损窗口、釜车邻接、温压积分、冻融损失和砌筑准入统一到一条可恢复的物料谱系中；它不复用既有项目的施工节点移交或一般出厂放行主题，并以“单坯体唯一来源、异常跨谱系传播、复验代次隔离和三终局竞争”形成独立业务识别度。
